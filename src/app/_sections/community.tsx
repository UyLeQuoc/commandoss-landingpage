export const CommunitySection = () => {
    return (
      <section id="community" className="min-h-screen flex items-center justify-center bg-gray-100">
          Community section(Not completed UI)
      </section>
    )
  }